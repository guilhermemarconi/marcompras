import { Story, Meta } from '@storybook/react/types-6-0';
import ListItem from '.';

export default {
  title: 'ListItem',
  component: ListItem
} as Meta;

export const Default: Story = () => <ListItem />;
